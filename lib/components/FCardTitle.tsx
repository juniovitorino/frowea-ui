import styled from 'styled-components';
import { theme } from '../styles/themes/light'

export const FCardTitle = styled.h5`
  font-size: 14px;
  line-height: 15px; 
  color: ${theme.colors.primary};
  font-weight: 500;
  padding: ${theme.spacing.base}
`
